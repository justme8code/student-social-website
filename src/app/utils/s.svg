<svg width="200" height="200" xmlns="http://www.w3.org/2000/svg">
    <!-- Outer Coin Circle -->
    <circle cx="100" cy="100" r="90" fill="#FFD700" stroke="#E5A100" stroke-width="5" />

    <!-- Inner Gradient -->
    <defs>
        <radialGradient id="coin_grad" cx="50%" cy="50%" r="50%" fx="50%" fy="50%">
            <stop offset="0%" stop-color="#FFF38E" />
            <stop offset="100%" stop-color="#FFD700" />
        </radialGradient>
    </defs>
    <circle cx="100" cy="100" r="85" fill="url(#coin_grad)" />

    <!-- Decorative Rings -->
    <circle cx="100" cy="100" r="70" fill="none" stroke="#E5A100" stroke-width="4" />
    <circle cx="100" cy="100" r="60" fill="none" stroke="#F6C700" stroke-width="2" />

    <!-- Letter K (Embossed Effect) -->
    <text x="100" y="115" text-anchor="middle" alignment-baseline="middle" font-size="90" font-family="Arial" fill="#D4A017" font-weight="bold" stroke="#E5A100" stroke-width="2">
        K
    </text>
</svg>
